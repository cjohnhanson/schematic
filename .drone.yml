kind: pipeline
name: schematic
steps:
  - name: garage_test
    image: python3:latest
    commands:
      - pip install requirements-dev.txt
      - pytest --cov=schematic --cov-fail-under=90
    when:
      event:
        - push
trigger:
  event:
    - push
